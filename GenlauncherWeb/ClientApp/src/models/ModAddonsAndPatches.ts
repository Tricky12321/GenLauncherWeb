export interface ModAddonsAndPatches {
  added: boolean;
  downloaded: boolean;
  installed: boolean;
  adding: boolean;
  modName: string;
  modLink: string;
  modPatches: string[];
  modAddons: string[];
}
